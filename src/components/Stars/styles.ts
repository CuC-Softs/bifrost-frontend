import styled from 'styled-components';

export const Container = styled.div`
  display: flex;

  svg {
    width: 2.5rem;
    height: 2.9rem;
    justify-self: flex-start;
    align-self: center;
  }
`;

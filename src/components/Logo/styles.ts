import styled from 'styled-components';

export const Container = styled.div`
  svg {
    width: 18.6rem;
    height: 9rem;

    @media (orientation: landscape) {
      width: 20.4rem;
      height: 10.3rem;
    }
  }
`;
